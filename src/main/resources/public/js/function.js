/*document.getElementById("enviar").addEventListener("click", function(){
    var usuario = document.getElementById('usuarioNuevo').value;
    var password = document.getElementById('contraNueva').value;
    var error = false; 
    if(usuario == ""){
        document.getElementById('usuarioNuevo').style.border= '3px solid red';
        error = true;
    }
    if(password == ""){
        document.getElementById('contraNueva').style.border= '3px solid red';
        error = true;
    }
    if (!error){
        console.log(usuario + password);
    }
    if(password != "" && usuario != ""){ //aca habria que chequear si es el usuario y contrasenia correcta
        window.location.href = "/incidentes";
    }
});*/
